import 'package:flutter/material.dart';

class CobranzaScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Cobranza'),
      ),
      body: Center(
        child: Text('Pantalla de Cobranza'),
      ),
    );
  }
}
